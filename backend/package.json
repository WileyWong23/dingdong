{
  "name": "dingdong-backend",
  "version": "1.0.0",
  "description": "叮咚 DingDong - 灵活定时提醒后端 API",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "hono": "^4.6.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/web-push": "^3.6.3",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
